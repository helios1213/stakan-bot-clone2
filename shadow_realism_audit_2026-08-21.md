# Shadow vs Live: чому shadow солодший і що з цим робити

Воркфлоу `shadow-realism-audit`, 2026-08-21. 40 агентів, 33 знахідки, 29 пережили
адверсарну перевірку. Робота велась ТІЛЬКИ на читання — слот 2 у цей час торгував
живими грошима.

## Що звірено вручну ПІСЛЯ воркфлоу (не вір агентам наосліп)
| твердження | результат перевірки |
|---|---|
| розрив за кількістю 16.8x / 10.9x | ✅ точно |
| розрив у bps 8.2x / 3.7x | ✅ точно |
| live SOXL ноціонал 2933 проти shadow 1470 | ✅ саме тому доларовий 1.85x занижував |
| `entry_slippage_pct` = 0 на ВСІХ live-рядках | ✅ 0 із 86 719 |
| комісії = 0 на всіх угодах | ✅ сума 0.0 |
| `max_concurrent_shadow_positions` — мертвий ключ | ✅ жодної згадки у `src/` |
| перетин `signal_uid` shadow/live = 0 | ✅ 0 при 34 138 / 125 567 |

---

## ПЛАН ДІЙ: чесний shadow

Усе нижче — тільки опис. Нічого не змінено, БД читалась read-only. Власна перевірка вікна (48г до `max(live_trades.closed_at)`=1787273964):

| | shadow n | live n | shadow bps | live bps | філ/спробу sh | філ/спробу lv | годин sh/lv |
|---|---|---|---|---|---|---|---|
| 1000PEPE | 9249 | 551 | 1.374 | 0.167 | 0.792 | 0.512 | 39 / 13 |
| SOXL | 10617 | 974 | 2.160 | 0.585 | 0.859 | 0.526 | 44 / 12 |

Заявлених у премісі 20x і 6.4x/1.9x у даних немає: реально 16.8x/10.9x за кількістю, а в bps нотіоналу 8.2x/3.7x (у доларах SOXL 1.85x лише тому, що live торгує нотіоналом 2933 проти 1470).

---

## 1. Розклад розриву

### (а) Кількість угод — тотожність сходиться точно

```
1000PEPE 16.8x = 3.00 (годин у режимі) × 3.61 (спроб/год) × 1.55 (філів/спробу)
SOXL     10.9x = 3.67                  × 1.82              × 1.63
```

- **Годин у режимі (3.0-3.7x) — артефакт вимірювання, не баг.** Пара або live, або shadow (`shadow_engine.py:2085-2113` — `[SKIP SHADOW]` виходить до `append(pos)`; роутинг за `pos.mode` на :3529). Слот один, оператор перекидає його руками (`state_transitions`, усі `manual_telegram`).
- **Спроб/год (1.8-3.6x) — переважно щільність сигналів у вибраних оператором вікнах** (PEPE live жив у нічні 01-09 UTC), плюс дрейф ціни PEPE +24% при фіксованих у тіках воротах. Гейти тут працюють **проти** shadow (спроб/сигнал shadow 0.34-0.39 проти live 0.46-0.60), тобто гейти розрив не створюють. Точний розподіл цього множника між «ринок» і «ворота» я не витягнув — див. §5.
- **Філів/спробу (1.55-1.63x) — ЄДИНИЙ реальний баг у (а).** Односпрямований на обох парах. Size-weighted ще гірше: 0.78-0.84 проти 0.44-0.51.

**Висновок (а): з 11-17x на модель припадає 1.6x. Решта — календар і ринок.** Жоден із «підозрюваних» (гейти, `max_concurrent_shadow_positions`, конкурентність, комісії) внеску не має; `max_concurrent_shadow_positions: 5` — мертвий ключ (`RiskConf` його ігнорує), реальний кеп 1/символ в обох, і він shadow **придушує**, а не роздуває.

### (б) PnL за угоду — розрив у bps: PEPE 1.21 bps, SOXL 1.58 bps

| канал | PEPE | SOXL |
|---|---|---|
| ціна входу (заморожений ліміт + самосуддя книгою) | 0.0-0.2 bps (**0-15%**) | 0.5-0.8 bps (**35-50%**) |
| пост-вхідна траєкторія (немодельована adverse selection / ціна на момент закриття) | 0.8-1.1 bps (**65-90%**) | 0.25-0.5 bps (**15-30%**) |
| зсув `opened_at_ms` (таймери виходу) | ~0.03 bps (**2-8%**) | ~0.05 bps (**2-8%**) |
| комісії, фандинг, плече, гейти, конкурентність, сайзинг у bps | **0** | **0** |
| **непояснений залишок** | **10-25%** | **20-40%** |

Сайзинг пояснює **розбіжність між заголовками пар**, а не сам розрив: на SOXL нормування на нотіонал розрив **збільшує** (1.85x → 3.7x). Часова диз'юнктність (а) на (б) **не переноситься** — на SOXL hour-match розрив зменшує, на PEPE збільшує до 6x; як пояснення непридатна, лише роздуває довірчі інтервали.

Виміряно й закрито: `exit_slippage_pct` у live — симульоване число, але воно не входить у жодну формулу (вплив $0.00); `entry_slippage_pct` у live тотожно 0 на всіх 86k рядках — теж write-only. Обидва — втрата спостережуваності, не грошей.

---

## 2. Зміни, ранжовані за (вплив на чесність)/(ризик для живої торгівлі)

### T0 — нульовий ризик для ордерного шляху (робити першими)

**T0.1 Записувати незаписані shadow-скіпи.** `shadow_engine.py`: гілки `return` на ~1419 і ~1428 (drift-гейт), плюс `should_reject_order` і `simulate_server_error` — продублювати INSERT з :1584-1591 з окремими `reason` (`latency_drift`, `sim_reject`, `sim_server_error`). Гілки під `not _is_live_pair`, live не зачіпає. Схема не змінюється (`reason` — текст).
*Перевірка:* сума нових рядків за годину ≈ `drift`/`rejected` у `[FUNNEL 60s]`; shadow fill-rate SOXL має впасти ~86% → ~82%, PEPE ~79% → ~78.9%.

**T0.2 Перерахувати `entry_slippage_pct` після живого філу.** `shadow_engine.py` одразу після :1952 — та сама формула, що на :1656/1658. Разом із цим: `place_ioc_open` має повертати фактичний `limit_scaled` (він рахується в `live_executor.py:1277-1280, але назовні не віддається) → нове поле в `LiveOrderResult`, і класти його в **нову** колонку `entry_limit_price`, а не перевизначати семантику `entry_target_price` (інакше історичні 86k рядків стають несумісні з новими).
*Перевірка:* `select count(*) from live_trades where entry_slippage_pct<>0` перестає бути 0; середнє має вийти ≈ +1.9 bps (SOXL) / +0.2 bps (PEPE) — тобто live філиться **гірше** ліміту, як і має бути в тейкера.
*Потребує:* ребілд. **Потребує міграції БД** (`entry_limit_price`).

**T0.3 Звітність у bps і на активну годину.** `scripts/analyze_pair.py`, панель: замість `$/угоду` — `sum(net_pnl)/sum(notional)*1e4`; замість `угод за 48г` — `філів на сигнал` і `угод на активну хвилину режиму`. Прибрати з усіх звітів `entry_slippage_pct` для live до T0.2.
*Перевірка:* SOXL перестає показувати «1.9x» і показує 3.7x — тобто звіт більше не ховає розрив за розміром.

**T0.4 Прибрати мертвий ключ** `config/config.yaml:139` (`max_concurrent_shadow_positions`) і згадку «shadow=5 одночасних» з опису моделі. **Не «лагодити» його підключенням** — з cap=5 shadow візьме ~+109k угод і розрив зросте до ~40x.

### T1 — змінює лише shadow-статистику (ордери не летять)

**T1.0 (передумова) Зміряти лаг WS-книги MEXC.** Без цього числа T1.1 некалібрований. Спосіб без живих ордерів: логувати в момент кожного live-філу вік останнього depth-апдейту `mexc_ob` і різницю між нашою книгою й ціною філа з біржі. Це єдиний спосіб розділити «фантомна ліквідність від лагу» і «рівень реальний, але його забрали швидші» — зараз вони сидять в одному залишку.

**T1.1 Прибрати тотожне скорочення лагу.** Зараз і якір ліміту, і суддя філа — одна й та сама реконструкція з `push.depth`: `_sig_limit` морозиться в t0 (`shadow_engine.py:1372-1388`), потім сон 150-205мс, потім `ioc_executor` ходить по **тій самій** книзі (`:1542`). Лаг фіду скорочується тотожно. Правка: новий параметр `shadow.mexc_feed_lag_ms`; `_sig_limit` лишається на t0, а walk виконується проти книги, зміщеної вперед на лаг (практично — подовжити сон перед walk'ом рівно на `feed_lag_ms`). Гілка `else` на :1541 — live туди не заходить (live має стаб на :1509-1540).
*Перевірка:* цільова метрика — **відношення філів/спробу shadow/live у спільних хвилинах** → має йти з 1.55-1.63 до 1.0-1.1.

**T1.2 Ліквідний хеаркат у `ioc_executor`.** `_simulate_buy_ioc:125-139` / `_simulate_sell_ioc` беруть **увесь** показаний `size` рівня. Додати `queue_frac` (частка рівня, доступна нам) і per-pair `p_miss` першого рівня, у `config/pairs/*.yaml` поруч з `ioc_offset_ticks`. Робити **тільки після T1.1** і калібрувати на залишок, бо інакше хеаркат просто замаскує лаг.
*Важливо:* бінарний факт філа в симуляторі вже тотожний реальному IOC (`min(_asks) <= limit`), тож walk впливає на **розмір**, а не на факт. Отже чесний хеаркат має бити насамперед по `filled_pct` (live PEPE 46% часткових проти shadow 5.7%) — і лише per-pair `p_miss` рухає сам fill-rate.
*Перевірка:* розподіл `entry_filled_pct` shadow має зійтися з live у спільних годинах; fill-rate — метрикою з T1.1.

**T1.3 Не філити по протухлій книзі.** У `simulate_ioc_entry` — якщо остання мітка апдейту `mexc_ob` старша за `entry_latency_ms`, повертати `expired`, а не проходити драбину. Дешево, ловить найгірший клас фантомів.

### T2 — змінює ПОВЕДІНКУ ЖИВОЇ ТОРГІВЛІ (окреме рішення оператора, не робити «заодно»)

**T2.1 Перештампувати `opened_at_ms` на реальному філі.** `shadow_engine.py`, у `if live_result.success:` поруч із :1952. Це не косметика: live зараз втрачає ~180мс із кожного вікна (`min_hold` 500, `nevergreen_cut` 900, DOA 2500/3000), і правка **дає живим угодам більше життя** — очікувано −5..−15 передчасних `nevergreen_cut`/`dead_on_arrival` за 48г. Ефект малий і в межах шуму, ризик — реальний.
*Безризикова альтернатива:* нова колонка `filled_at_ms` + використання її **лише** в аналітиці (`peak_ticks_at_*`, `duration_sec`), таймери не чіпати. **Потребує міграції БД.**
*Перевірка:* `min(duration_ms)` по `simple_stalled` у live має зійтися з shadow (зараз +222мс).

**T2.2 Shadow-двійник для live-пар.** Замість `return` на :2085-2113 писати паралельний рядок з `mode='shadow_parallel'` і тим самим `signal_uid`. Це **структурний** фікс: без нього кожне наступне порівняння міряє календар (зараз перетин `signal_uid` = 0 на 1113 живих рядках, попарне зіставлення неможливе в принципі).
*Ризики, які треба зняти ДО впровадження:* (1) walk книги коштував 5-15мс на сигнал — саме тому його й прибрали з live fast-path (коментар :1502-1508); двійника рахувати **після** відправки живого ордера, окремою задачею, ніколи в шляху до `submit`; (2) `shadow_parallel` не має потрапляти в жоден агрегат/алерт/промоушен — інакше отруїть відбір пар. **Потребує міграції БД** (індекс/фільтр за `mode`) і ребілду.

### НЕ робити

- Не чіпати `entry_latency_min_ms`/`max_ms` (150/205 проти реальних p10/p90 156/204 — калібровано добре, стеля виграшу ≤2 в.п.).
- Не шукати нічого в комісіях/фандингу: 0 на всіх 208k угодах, фандинг-поріг 36с при max holding 34.3с за всю історію.
- Не послаблювати `slot_open_cooldown` заради кількості: стеля +23% live-угод, розрив 16x→13x.
- Не «вирівнювати» `SHADOW_MARGIN_*` під `slot_pair_sizing` (варіант T0.3 дешевший і не ламає стабільність бенчмарку). Але тоді ніколи не порівнювати $/угоду.

---

## 3. Порядок і критерій приймання

Один критерій, зафіксований **до** правок: на парі, що була live у тому ж вікні, у спільних хвилинах —
- `філів/спробу shadow / live` → **1.0 ± 0.1** (зараз 1.55-1.63)
- `bps shadow / live` → **≤ 1.3** (зараз 3.7-8.2)

Порядок: T0.1 → T0.3 (робить розрив видимим чесно) → T0.2 → T1.0 (замір лагу) → T1.1 → T1.3 → T1.2 (калібрування на залишок) → окреме рішення по T2.

Ребілд потрібен на будь-яку зміну коду (джерела не змонтовані). Міграція БД потрібна для: `entry_limit_price` (T0.2), `filled_at_ms` (T2.1-альтернатива), `mode='shadow_parallel'` (T2.2). Дзеркалити на srv1 і пушити в обидві репи — правки в `shadow_engine.py`/`ioc_executor.py` це спільний виконавчий код.

---

## 4. Чого НЕ вдалось пояснити

1. **Залишок 20-40% (SOXL) і 10-25% (PEPE) розриву PnL/угоду** — без атрибуції.
2. **Розщеплення ~20 п.п. fill-rate між «лаг WS-фіду» і «черга/конкуренція за показаний рівень»** — принципово неможливе на наявних даних, обидва сидять в одному залишку. Це і є задача T1.0.
3. **Механізм PEPE** — 65-90% його розриву сидить «після входу», але дві незалежні перевірки атрибутують по-різному (adverse selection всередині бакета проти ціни на момент закриття), і жодна не відтворюється на SOXL. Атрибуція нестабільна між парами.
4. **Множник «спроб/год» (1.8-3.6x)** — не розділений між щільністю сигналів, епохою `bookTicker`-фіду і дрейфом ціни PEPE (+24% при воротах, фіксованих у тіках).
5. **Ендогенність промоушену**: оператор вмикає live **після** хорошого shadow-вікна → регресія до середнього систематично робить live гіршим. Механізм реальний, не виміряний, і працює в той самий бік, що й усі знахідки.
6. **Довірча база**: 2 пари, live активний 12-13 годин із 48, спільних годин 3-7. Кілька per-pair атрибуцій міняють знак при зміні вікна на ±2 години (зокрема вихідна нога SOXL). Жодну цифру з розділу (б) не варто цитувати з точністю кращою за «десятки відсотків», доки не працює T2.2.