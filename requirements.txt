# Core async
aiohttp==3.10.10
websockets==13.1
uvloop==0.21.0

# Telegram
python-telegram-bot[ext]==21.6

# Data and config
pyyaml==6.0.2
pydantic==2.9.2
pydantic-settings==2.6.0

# Crypto for API key encryption
cryptography==43.0.3

# Webkey signing (MEXC futures via cookie auth) + Akamai TLS bypass
pycryptodome==3.21.0
curl_cffi==0.15.0

# DB
aiosqlite==0.20.0

# MEXC official lib (we'll use direct REST too, but this helps for some endpoints)
# Note: MEXC has no first-party Python SDK for Futures; we'll write thin wrappers.
# For Binance we use raw WebSocket (no SDK needed).

# Utilities
orjson==3.10.10
loguru==0.7.2
tenacity==9.0.0


# Dev
pytest==8.3.3
pytest-asyncio==0.24.0
