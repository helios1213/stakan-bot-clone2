[pytest]
asyncio_mode = auto
# Pin to current behaviour (per-test event loop for async fixtures). Without
# this, future pytest-asyncio versions change the default and can re-break
# async-fixture tests (e.g. test_webkey_credentials).
asyncio_default_fixture_loop_scope = function
testpaths = tests
