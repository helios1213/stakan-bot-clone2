meta:
  symbol: SOXLUSDT
  behavioral_epoch: 1787325235
  note: |
    STOCK-перп, доданий у SHADOW 2026-08-11. SOXL (Direxion 3x semis ETF) stock-perp; MEXC=SOXL_USDT
    Ворота стартові (~2.1bps геп), історії немає — перетюнити з
    shadow-даних. behavioral_epoch бампати лише на реальну зміну тюнінгу.
    2026-08-12: РАЗОМ min_ticks 4->5 (ріже gap4-когорту, 68% обсягу, -$1.69,
    мінус в обидві ери) + trail breakeven_trigger_bps 5->1.5 і trail_distance_bps
    3->2 (брейкивен був фактично мертвий: вмикався 6/81). Два важелі одночасно.
detector:
  min_ticks: 5
  cooldown_sec: 3.0
  long_only: false
  short_only: false
  max_spread_ticks: 4
  max_spread_bps: 0.0
  min_mid_gap_ticks: 2.75
exit_strategy:
  min_hold_ms: 500
  stop_adverse_bps: 10.0
  trail_distance_bps: 2.0
  breakeven_trigger_bps: 4.0  # 2026-08-13: 1.5->4.0 — прибрати шкоду BE (зводився на +1.5bps і стопив winners у беззбиток, мінус на обох ботах, workflow Rank2)
  stall_timeout_ms: 3000
  dead_on_arrival_timeout_ms: 2500
  nevergreen_cut_ms: 900
  nevergreen_adverse_ticks: 3.0
  nevergreen_peak_ticks: 1.0
  binance_reversal_max_ms: 1500
execution:
  ioc_offset_ticks: 3
  ioc_max_attempts: 1
  ioc_attempt_interval_ms: 0
  stop_loss_ticks: 6
  sl_grace_sec: 1.5
  max_hold_sec: 60
  # Підняті з 3/2 до 5/5 (2026-08-21) як ЕКСПЕРИМЕНТ проти api_error_9082.
  # Гейтять лише перевхід ПІСЛЯ закритої позиції — протухлі IOC (30% наших
  # запитів) вони не чіпають, для тих потрібен cooldown_sec вище.
  # Виміряно на слоті 2 (15 539 угод): відсікання перевходів швидших за 6с
  # прибирає 12.3% запитів і 33.5% PnL, АЛЕ 82.8% тієї вартості — один день
  # 08-21; медіанний день коштує $3.39. Тобто у звичайний день це дешево, а
  # на бурстовому — дорого, і саме бурст нас троттлить.
  # ЗАСТЕРЕЖЕННЯ: зв'язок частоти з 9082 напрямком підтверджується, але
  # ПОРОГА в даних немає (08-11: 42 запити/60с — вільний; 08-04: 10/60с —
  # обмежений). Це експеримент, а не виправлення. Читати за: епізодів 9082
  # на день і PnL/день проти попереднього періоду.
  cooldown_after_loss_sec: 5
  cooldown_after_win_sec: 5
  binance_reversal_ticks: 0.0
  gap_retrace_frac: 0.0
  min_mexc_lag_pct: 0.0
