# ZECUSDT — SELF-CONTAINED config (all fields explicit; no global.yaml inheritance).
# Materialized from the effective runtime config. Edit here directly per-pair.

meta:
  symbol: ZECUSDT
  behavioral_epoch: 1785081039
  note: behavioral_epoch = unix time the TUNING last changed; bump ONLY on a real
    config change, NOT on re-materialization. Analyze only trades with opened_at >=
    this.
detector:
  min_ticks: 10     # 2026-07-17 raised to fill-floor (ZEC fast: touch~134ms, only >=11t fill)
  cooldown_sec: 1.0
  long_only: false
  short_only: false
  max_spread_bps: 0.0
exit_strategy:
  min_hold_ms: 500
  stop_adverse_bps: 1.5
  trail_distance_bps: 5.0
  breakeven_trigger_bps: 5.0
  stall_timeout_ms: 3000
  dead_on_arrival_timeout_ms: 3000
  nevergreen_cut_ms: 500
  nevergreen_adverse_ticks: 5.0
  nevergreen_peak_ticks: 1.0
  binance_reversal_max_ms: 1500
execution:
  ioc_offset_ticks: 5      # 2026-07-17 (was 3) more aggressive cross for fills
  ioc_max_attempts: 1
  ioc_attempt_interval_ms: 80
  margin_min_usdt: 3.0
  margin_max_usdt: 4.0
  leverage_min: 35
  leverage_max: 40
  stop_loss_ticks: 0
  sl_grace_sec: 0.0
  max_hold_sec: 60
  cooldown_after_loss_sec: 5
  cooldown_after_win_sec: 5
  binance_reversal_ticks: 20.0
  gap_retrace_frac: 0.0
  momentum_filter: false
  momentum_tau_sec: 2.0
  momentum_threshold_bps: 2.0
  min_mexc_lag_pct: 0.02
